requests>=2.27.1,<3.0.0  # 用于发送HTTP请求获取数据
jinja2>=3.0.6,<4.0.0     # 用于模板渲染
python-dateutil>=2.8.2,<3.0.0  # 用于日期和时间处理（可能用于计算配置文件的过期日期）

# 可选依赖（根据实际项目需求）
schedule>=1.1.0,<2.0.0  # 如果使用此库实现定时任务
loguru>=0.5.3,<1.0.0    # 如果使用此库进行日志记录